package org.taskmastes.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SecurityConfig {
    // Seu código atual (desativado para ignorar as configurações)
}